# Real config may contain private URLs / credentials — keep only the example.
config.yaml

# Build output
/bin/
/monitor
*.exe

# Env
.env

# Terraform: never commit state or secrets (the .lock.hcl SHOULD be committed)
deploy/terraform/.terraform/
deploy/terraform/*.tfstate
deploy/terraform/*.tfstate.*
deploy/terraform/terraform.tfvars
deploy/terraform/*.tfplan

# macOS
.DS_Store
